# ============================================================
# requirements.txt — RESNET_USADA (Multi-Model: ResNet50, MobileNetV3, ViT)
# Install: pip install -r requirements.txt
# CUDA version: pastikan PyTorch versi CUDA sesuai GPU kalian
# Cek di: https://pytorch.org/get-started/locally/
# ============================================================

# Deep Learning
torch>=2.1.0           # PyTorch core (install versi CUDA dari pytorch.org)
torchvision>=0.16.0    # transforms, pretrained models (ResNet50, MobileNetV3, ViT-B/16)
torchaudio>=2.1.0      # opsional, ikut paket torch

# Data & Numerik
numpy>=1.24.0
Pillow>=10.0.0
scikit-learn>=1.3.0

# Visualisasi
matplotlib>=3.7.0
seaborn>=0.12.0

# FLOPs counter (untuk ResNet50, MobileNet, dan ViT — semua didukung thop)
thop>=0.1.1

# Utilitas
tqdm>=4.65.0
